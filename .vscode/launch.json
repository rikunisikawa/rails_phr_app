{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "rdbg",
      "name": "Attach to Rails server (Docker)",
      "request": "attach",
      "debugPort": "localhost:12345",
      "env": {
        "PATH": "/usr/local/bin:${env:PATH}"
      }
    }
  ]
}